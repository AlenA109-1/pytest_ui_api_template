import requests

from configutation.ConfigProvider import ConfigProvider

class SearchApi:
    def __init__(self, base_url: str) -> None:
        self.base_url = base_url

        #подложить куки

        #написать проверки, создать кортежи тестовых данных @

        #search_by_namebook
        #search_by_author
        #search_by_num

        #put_into_cart
        #go_to_cart
        # возможно, проверить размер текста в каких-то карточках/блоках, или размер иконок

